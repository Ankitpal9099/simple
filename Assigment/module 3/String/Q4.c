// 4. Write a program in C to count the total number of words in a string.
#include<stdio.h>
#include<string.h>
void main()
{
    char str[10];
    int count = 0;
    printf("Enter a string: ");
    gets(str);
    printf("string is length %d",strlen(str));
    
}